%h1=t I18n.t("_login.signup")
= error_messages_for :user

- form_for(@user, :html => { :multipart => true }) do |f|
  = render :partial => 'form', :locals => {:f => f}
  
  %p
    = f.submit I18n.t("new.submit")
